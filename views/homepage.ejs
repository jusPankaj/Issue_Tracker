<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <!-- <title><%= title %></title> -->

    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
    />

    <!-- Custom Styles -->
    <style>
      /* Add your custom styles here */
      /* Example: .card { background-color: lightblue; } */
      body {
        /* padding-top: 70px; Add padding-top to account for fixed header */
        background-color: #f8f9fa; /* Light gray background color */
      }
      /* Adjust padding and margin for content */
      .container {
        margin-top: 20px;
        margin-bottom: 20px;
      }
      .card {
        background-color: #f0f8ff; /* Light blue background color */
        border: 1px solid #cfd8dc; /* Light gray border */
        border-radius: 8px; /* Rounded corners */
      }
      .card-header {
        background-color: #90caf9; /* Light blue header background color */
        color: #fff; /* White text color */
        font-weight: bold; /* Bold font weight */
        border-bottom: none; /* Remove bottom border */
      }
      .card-body {
        padding: 20px; /* Add padding to the body */
      }
      .card-text {
        margin-bottom: 0; /* Remove bottom margin for text */
      }
      .btn-warning {
        background-color: #ffb74d; /* Orange button background color */
        border-color: #ffb74d; /* Orange button border color */
      }
      .btn-warning:hover {
        background-color: #ffa726; /* Darker orange on hover */
        border-color: #ffa726; /* Darker orange border on hover */
      }
    </style>
  </head>
  <body>
    <!-- Main Content -->
    <div class="container">
      <div class="mb-5 p-5">
        <% for( let project of projects ) { %>
        <div class="card mb-4" style="background-color: #182034; color: wheat">
          <div class="card-header">
            <strong>Name of the project:</strong>
            <%= project.name %>
          </div>
          <div class="card-body">
            <h6 class="card-subtitle mb-2">
              <strong>Author:</strong>
              <%= project.author %>
            </h6>
            <p class="card-text">
              <strong>Description:</strong>
              <%= project.description %>
            </p>
            <div class="text-end">
              <a
                href="/project/<%= project._id %>"
                class="btn"
                style="font-weight: bold; color: #007bff"
                >Open Project</a
              >
            </div>
          </div>
        </div>
        <% } %>
      </div>
    </div>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
